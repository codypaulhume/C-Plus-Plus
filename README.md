This project is a least recently used cache. It uses a doubly linked list and an unordered map to keep track of recently used values and a short lookup respectively.
I created the DLL from scratch and used the unordered map from the C++ library. A wrapper class was used to utilize both.

Modify the main function to run
